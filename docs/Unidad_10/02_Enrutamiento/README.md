# Sección 2: Método de Enrutamiento

El enrutamiento es el mecanismo que conecta la petición del usuario con la lógica de la aplicación. Esta sección detalla cómo construir un Router dinámico que interprete URLs basadas en convenciones.

## Contenidos

1.  **[Método de Enrutamiento](./10.2_Metodo_Enrutamiento.md)**: Configuración del `.htaccess` para redirección, creación de la clase `Router` para el parsing de URLs y la lógica de despacho dinámico usando `call_user_func_array`.
